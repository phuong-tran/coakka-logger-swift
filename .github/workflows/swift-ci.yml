name: swift-ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  swift:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Smoke package
        run: bash scripts/smoke-package.sh
